[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200

[profile.default.fuzz]
runs = 1000

[profile.ci]
fuzz = { runs = 10000 }

[rpc_endpoints]
sepolia = "${SEPOLIA_RPC_URL}"